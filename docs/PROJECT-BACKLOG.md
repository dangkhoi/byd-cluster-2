# ClusterNav 2.0 — Project Backlog

> **Trạng thái**: Current · **Cập nhật**: 2026-08-20 · **Mục đích**: Nguồn DUY NHẤT cho task (ID · việc · trạng thái · ngày bắt đầu/kết thúc).

> File quản lý công việc chung của dự án. Cập nhật status khi làm.
> **Legend:** ✅ DONE · 🔧 REFINE (đã làm, cần chỉnh) · 🔲 TODO (làm off-car được) · ⛔ BLOCKED (cần xe / anh em) · 📋 BACKLOG (ưu tiên thấp)
> Branch làm việc: `feat/speed-limit-badge-hal-hud` (main = `f7843c0` 1.0 ổn định, KHÔNG đụng đến khi PASS on-car).

---

## A. ĐÃ XONG (DONE)

| ID | Việc | Trạng thái | Bắt đầu | Kết thúc | Ghi chú |
|----|------|-----------|---------|----------|---------|
| A1 | Badge placement UI (kéo-thả) + gom 1 overlay + full VietMap logging + Waze tag fix | ✅ | 2026-08-17 | 2026-08-18 | commit `d4dd6d5` |
| A2 | Badge lifecycle fix (idempotent init + DisplayListener) + badge toggle + nav_access source-tag + screenshot fission-index + VietMap alert fix | ✅ | 2026-08-18 | 2026-08-18 | commit `bd09a4d` |
| A3 | Bind VMAlertWidgetProvider → capture "giới hạn sắp tới + cự ly" (upLimit/upDist) | ✅ | 2026-08-18 | 2026-08-18 | commit `36f6bf0`; V3 PASS (data thật 50/60/70/80 + đếm lùi) |
| A4 | Raw-notif capture (mọi notif của 5 gói, tag pkg) | ✅ | 2026-08-18 | 2026-08-18 | commit `16f7214` |
| A5 | a11y contentDescription capture (VietMap phơi nav ở content-desc, không phải text) | ✅ | 2026-08-18 | 2026-08-18 | commit `53265c5`; verify on-car; **chỉ foreground** |
| A6 | Per-app/per-channel log analyzer (`scripts/analysis/analyze_drive_logs.py`) | ✅ | 2026-08-18 | 2026-08-18 | commit `d6de8fd` |
| A7 | HUD-compare toolkit (navopen-v4 getraw) + guide Windows + thêm cờ 20/22/18 | ✅ | 2026-08-18 | 2026-08-18 | commit `ab56362`,`7027a21`; gửi anh em |
| A8 | Logging/ảnh mặc định OFF + storage cap <150MB | ✅ | 2026-08-18 | 2026-08-18 | commit `11751ba`; fix tràn bộ nhớ xe |
| A9 | Upcoming speed-limit badge (badge thứ 2 + cự ly) | ✅ | 2026-08-18 | 2026-08-19 | commit `11751ba`; design chỉnh xong ở B2 (xám/80%/45° dưới-trái) |
| A10 | Validate mũi tên 1-1 (18/18 đúng) + nội suy cự ly (đúng, bám notif) | ✅ | 2026-08-18 | 2026-08-19 | commit `1d2387f`; docs/diagnostics/ |
| A11 | HUD baseline read (0x38B00030 = NOT provisioned) + write-attempt (mọi write bị từ chối → coding-locked) | ✅ | 2026-08-18 | 2026-08-18 | on-car; `~/Desktop/hud-xe-minh.txt` |
| A12 | Push feat lên origin (public) + scrub topology | ✅ | 2026-08-19 | 2026-08-19 | `3745046`; zero IP footprint |

---

## B. TODO — off-car (làm được ngay)

| ID | Việc | Trạng thái | Bắt đầu | Kết thúc | Ghi chú |
|----|------|-----------|---------|----------|---------|
| B1 | **Auto-start VietMap khi mở app** — NẾU bật hiện speed badge thì tự mở VietMap lúc mở app mình (để widget có nguồn) | ✅ | 2026-08-19 | 2026-08-19 | MainActivity.maybeAutoStartVietMap (gate badgeEnabled + installed-check, degrade-safe) |
| B2 | **Chỉnh design badge "giới hạn sắp tới"**: viền/số XÁM, 80%, chéo 45° dưới-trái | ✅ | 2026-08-19 | 2026-08-19 | SpeedBadgeView muted flag + Overlay 45° lower-left 80%; badge chính KHÔNG đổi; spec updated |
| B3 | **Screen-capture + xử lý ảnh (học OpenBYD `WazeArrowCaptureService`)** → nguồn **Waze arrow** + **camera VietMap**. MediaProjection + PixelCopy + phân tích pixel mũi tên/icon (như ManeuverSignature) | 🔧 | 2026-08-19 | | Spec DUYỆT + committed `e914642`. **08-20 test emulator NGHIÊM TÚC + FIX (sub-agent DAG):** B3.5 (a11y-bounds) + B3.7 (overlay filter + rootInActiveWindow bootstrap) + B3.9 (rect **tight arrow-only** 960×720 `(38,38,120,110)`) DONE; **B3.6: template Waze rẽ-TRÁI thu qua `signatureBits` → Waze classify ĐÚNG `amap=2` END-TO-END** (trước khớp nhầm ROUNDABOUT). B3.8 (VietMap 2-target ARROW+CAMERA) code+unit xong nhưng **emulator bị B3.10 chặn** (`SourceArbiter.activeSource` dính WazeMod sau force-stop). Còn lại: mở rộng template đủ maneuver/app (B3.6), template camera VietMap (OQ4), fix B3.10. Build xanh. |
| B3.1 | Case 1: app dẫn đường **full màn chính** → mirror màn chính, crop vùng mũi tên | ⛔ | | | transport `fission -d1` impl (e914642); **verify on-car** |
| B3.2 | Case 2: app dẫn **1/2 màn chính** (trái HOẶC phải — màn chính chia đôi) → mirror + crop đúng nửa | ⛔ | | | transport + half-offset (CaptureRouter) impl; **verify on-car** |
| B3.3 | Case 3: app dẫn **bên màn cụm** (đã cast) → chụp cụm | ⛔ | | | impl = `fission -d0` (cụm, proven); PixelCopy-SurfaceView = nhánh tương lai (spec §4.3 delta); **verify on-car** |
| B3.4 | Case 4: app **không active đâu** → offscreen VD + ImageReader (học OpenBYD) | ⛔ | | | scaffold `OffscreenMirrorCapturer` (MediaProjection AUTO_MIRROR). ⚠ **B3.12 BÁC:** AUTO_MIRROR mirror màn FOREGROUND, KHÔNG đọc được app BACKGROUND → case-4 **bất khả bằng screen-capture**; muốn đọc background phải VirtualDisplay riêng (phức tạp) hoặc dùng NOTIFICATION. Xem B3.12 |
| B3.5 | **[BUG · P1]** a11y-dynamic bounds chộp SAI node — nhắm mũi tên/camera nhưng lấy element vụn (18×19, 62×62: biển báo / mảnh xe) → crop rác → classify null | ✅ | 2026-08-20 | 2026-08-20 | `CaptureBoundsHeuristic.pick()` viết lại (min-size 32×24 loại junk + score size/zone/aspect) + gather-side min-size filter; +6 unit test (repro 18×19+62×62 → chọn banner). Off-car proven |
| B3.6 | **[GAP · P1]** Registry classify KHÔNG có glyph Waze (`ManeuverRegistry` toàn icon AMAP/Mapbox) → mũi tên Waze luôn null/sai dù crop đúng | 🔧 | 2026-08-20 | | **Cơ chế XONG + Waze rẽ-TRÁI PROVEN end-to-end** (`WazeArrowRegistry` + `signatureBits` + tight-crop → `amap=2` ĐÚNG). **08-20 T7: 2 template LEFT** (biến thể glyph) → arrow(image) amap=2 → owner ACTIVE. CÒN: thu đủ maneuver (phải/thẳng/quay-đầu/vòng-xuyến) + VietMap arrow (thu template khi gặp glyph mới) |
| B3.7 | **[BUG · P2]** Nhiễu đa-app: overlay nổi WazeMod đè lên → B3 nhận nhầm app (VietMap → ARROW thay vì CAMERA) | ✅ | 2026-08-20 | 2026-08-20 | `ForegroundWindowFilter` + rootInActiveWindow bootstrap; **giải triệt để bằng `NavWindowPicker` (B3.13)**: lọc TYPE_APPLICATION + chọn nav-window LỚN NHẤT → overlay/widget nổi bị loại theo type/size. Verified emulator: VietMap-widget `t3` 202×147 bị loại, Waze `t1` 960×720 được chọn |
| B3.8 | **[GAP/DESIGN · P2]** VietMap đọc CẢ mũi tên (banner trái) LẪN camera | 🔧 | 2026-08-20 | | Code+unit XONG (`targetsForPackage` VietMap→[ARROW,CAMERA] + `routePlans` + tick per-target degrade-safe). Emulator CHƯA verify (bị B3.10 chặn pkg). Camera neo-bản-đồ di chuyển + 0 template = OQ4 |
| B3.9 | **[CALIB]** Rect mũi tên tight arrow-only `(38,38,120,110)` @960×720 (đo THẬT emulator) | ✅ | 2026-08-20 | 2026-08-20 | Verified: crop trúng glyph → signature dày → classify được (banner 182×80 làm mũi tên nét-mảnh biến mất). Seed OpenBYD giữ làm null-default; xác nhận màn XE khớp 960×720 (OQ2) |
| B3.10 | **[BUG · P2]** `SourceArbiter.activeSource` dính nguồn cũ (poll còn tươi) → B3 route sai pkg | ✅ | 2026-08-20 | 2026-08-20 | Fix ở resolve pkg trong `ScreenCaptureNavSource`: CAPTURE route theo **CaptureForegroundSource (B3.13 window-enum) khi tươi** (app đang HIỂN THỊ) rồi mới tới activeSource → không bị nguồn giữ-khoá-cụm stale ép route sai. Build xanh | Found emulator 08-20; arbiter nên nhường foreground khi nguồn data stale / app đã tắt; off-car được |
| B3.11 | **[BUG · P1]** FALSE-POSITIVE khi crop TRỐNG: signature toàn-0 vẫn khớp template thưa (Hamming 0→≤18) → tín hiệu rẽ GIẢ | ✅ | 2026-08-20 | 2026-08-20 | Guard **MIN_SIG_BITS=10** trong `ManeuverSignature.match()` (template thật ≥18 bit; crop trống ~0-4) → query thưa trả null. +test (nền đen+4 cell trắng → classify null). matchNCC đã tự chặn all-zero (variance<1e-6). | Found emulator 08-20 (test background). Guard: chặn signature quá THƯA (số bit set < ngưỡng) → trả null; hoặc yêu cầu density/Hamming-quality tối thiểu. `contrast<30` chưa đủ. Off-car test được |
| B3.12 | **[LIMIT · nền tảng — CHỈ kênh ẢNH]** Kênh **screen-capture (mũi tên)** KHÔNG đọc được app khi BACKGROUND — `screencap`/AUTO_MIRROR chỉ chụp màn foreground. **⟵ Kênh TEXT thì ĐỌC ĐƯỢC**, xem B3.13 | 🔲 | 2026-08-20 | | Verified emulator (HOME→sig toàn-0). Mũi-tên-ẢNH cần pixel Waze trên display chụp được. Nhưng OpenBYD chứng minh **TEXT** (cự ly/đường/ETA) đọc được khi Waze KHÔNG foreground qua a11y window-enum (B3.13). Kết luận "background bất khả" ban đầu SAI cho text. |
| B3.13 | **[FIX · P1 — cốt lõi] Đọc nav app KHI KHÔNG foreground (như OpenBYD)** — bật lại `flagRetrieveInteractiveWindows` + duyệt `getWindows()`/`getWindowsOnAllDisplays()` tìm window nav app (pkg+bounds+displayId) bất kể foreground → đọc/gate theo nó | 🔧 | 2026-08-20 | | **IMPL + PROVEN emulator 08-20:** bật cờ + `NavWindowPicker` (thuần, 6 test) chọn TYPE_APPLICATION nav-window lớn nhất, bất kể focus. Log thật: `getWindowsOnAllDisplays` trả 10 window (trước rỗng) → **pick=wazemod** (t1 960×720), loại VietMap-widget t3 + systemui. `resolveNavWindowRegardlessOfFocus` (throttle 800ms) trong `NavAccessibilityService` → `CaptureForegroundSource`. **CÒN:** chạy trong onAccessibilityEvent → chỉ khi nav app bắn event; Waze idle-không-focus hoàn toàn cần TIMER (refine). Nguồn: `openbyd-2.3 BydAccessibilityService.rootNodeForPackages` |
| B3.14 | **[full-B3 · T1] Đọc LÀN đường** — LaneInfo + LaneSignature (:core) + a11y viewId bounds → crop → publishLane | 🔲 PARK | 2026-08-20 | | Contract+classifier+capture XONG (bbee318/20d56a1) nhưng **OWNER DE-PRIORITIZE (08-20 cuối phiên)**: làn KHÔNG phơi ở NỀN bởi bất kỳ app nào → **bắt buộc app VISIBLE** (foreground/cast) để capture (xem diagnostics multi-app-nav-source-channels). Park tới khi cần ca app-visible-trên-cụm |
| B3.15 | **[full-B3 · T4] Output owner → cụm/HUD payload** — NavOutputOwner đọc ScreenCaptureSignal → BydHal pushNavigation/pushLane/pushCamera (CONTENT-only, keep-alive 250ms, không gate chéo) | ✅ | 2026-08-20 | 2026-08-20 | db0b546. writeNavFrame KHÔNG đụng (owner data-path độc quyền, PhysicalHudOwnershipTest xanh). Emulator: owner ACTIVE decided sink=true (push thật = on-car, HAL device null off-car) |
| B3.16 | **[full-B3 · T5] Overlay cụm** — NavClusterOverlay (display-1): dải làn dưới speed-badge + chip camera phải speed-sign, tái dùng cơ chế SpeedBadgeOverlay (ClusterOverlayHost) | ✅ | 2026-08-20 | 2026-08-20 | db0b546. KHÔNG đụng SpeedBadgeOverlay. **RENDER PROVEN trên display 0** (Prefs.overlayDisplayId + inject frame debug → sub-agent đọc ảnh: dải làn 4-cell `← mờ·↑ sáng·↑ sáng·→ mờ` + chip camera "300 m" vẽ đúng). display-1 cụm thật = on-car (chỉ khác display id). Placement fraction tinh chỉnh on-car (R6) |
| B3.17 | **[full-B3 · T3] Menu chọn nguồn** — spinner AUTO/GMaps/Waze/VietMap → Prefs.setSourceMode + dòng trạng thái nguồn đang dẫn (NavSourceLabels) | ✅ | 2026-08-20 | 2026-08-20 | db0b546 + **P0 fix 5d4dab6** (thiếu txt_nav_source_active trong layout-w960dp → MainActivity crash màn rộng). Verified LIVE emulator: uiautomator dump thấy spinner='Tự động' + 'Đang dẫn: —' |
| B3.18 | **[full-B3 · T6+T7] Wire lifecycle + end-to-end** — listener khởi/dừng owner+overlay (gate Prefs.enabled, onDestroy close) + emulator end-to-end | ✅ | 2026-08-20 | 2026-08-20 | a1eca3a+6e6302c. **Arrow PROVEN emulator**: Waze LEFT→capture→pick=wazemod→classify amap=2→publishArrow→owner ACTIVE. Senior review APPROVED (0 P0/P1; [P2] onDestroy-leak PATCHED). [P2 on-car]: guard issueClear khi DATA tươi (chống chớp centre-nav). Overlay render + HAL push thật = on-car |
| B3.19 | **[FINDING · 08-20] Ma trận kênh nguồn + KHUNG ĐÚNG (data, không phải overlay)** — chốt bằng bằng chứng: mỗi app phơi gì ở NỀN vs cần VISIBLE | ✅ | 2026-08-20 | 2026-08-20 | Doc `docs/diagnostics/multi-app-nav-source-channels-2026-08-20.md`. KHUNG: GMaps=dẫn (notif→HUD cụm) · VietMap=DATA VN (widget→overlay MÌNH), SONG SONG. NỀN: tốc độ+camera/cảnh-báo(cự-ly) từ VietMap widget được; **turn-glyph+LÀN bất khả ở nền** (cần visible). Bác: lừa/di-chuyển/clone overlay VietMap lên cụm (overlay hardcode display 0, không reparent được; clone=thừa) |
| B3.20 | **[feat · NEXT] Chip camera/cảnh-báo VietMap trên cụm** — đọc `VietMapRoadAlert` (cự-ly + ảnh) → vẽ chip cạnh speed badge, SONG SONG GMaps-nav + badge tốc độ (đã chạy) | 🔲 | 2026-08-20 | | Ưu tiên **widget** (bền, không cần bong bóng bật). Loại "camera" cụ thể = match hash icon (thu 1 lần). Đây là việc THỰC CHẤT còn thiếu để đủ "data VN trên cụm" |
| B3.21 | **[DECISION] Waze HLP-logcat = hardware-gated** — HudLink chỉ EMIT khi có HUD BT/BLE peer; head unit trần → rỗng, chưa proven | 🔲 | 2026-08-20 | | KHÔNG để "Waze (HLP)" là nguồn đang-chạy trong options; nếu giữ → gắn nhãn "cần HUD BLE". HLP không có field làn. Nguồn: WazeHudSource/WazeManager |
| B3.22 | **[UX] Tách nguồn: Nav (trái→HUD) vs Info (phải→overlay: tốc-độ/camera)** — khớp two-track + layout 2 cột sẵn có | BACKLOG | 2026-08-20 | | spinner_nav_source (trái) + spinner_speed_source (phải+badge) đã partial. Làn/camera chưa selectable riêng; camera nên vào nhóm info (phải). Design bàn 08-20; chưa implement |
| B3.23 | **[CALIB · on-car 08-21] Recalib crop theo kích thước THẬT** — cụm **1920×720**, chính **1920×1080** (emulator 960×720 SAI) + trừ offset chrome/letterbox của GMaps-cast trên cụm | 🔲 | 2026-08-21 | | Ground truth read-only qua adb: cụm = fission virtual 1920×720; ClusterOverlayHost default 1920×720 KHỚP ✓ nhưng rect mũi tên B3.9 (960×720) phải recalib. Setup owner: GMaps cast cụm (windowed) + badge VietMap màn chính (chạy tốt). Xem diagnostics multi-app-nav-source-channels §8 |
| B4 | Diagnostics hygiene: đánh dấu `screenRead` INVALID khi stale (age cao) / không có road (GMaps nền) | ✅ | 2026-08-19 | 2026-08-19 | freshScreenRead (age>2500ms/no-road→-1) + refine guard reject stale; 8 test mới |

---

## C. ON-CAR / BLOCKED (cần xe owner hoặc anh em)

| ID | Việc | Trạng thái | Bắt đầu | Kết thúc | Ghi chú |
|----|------|-----------|---------|----------|---------|
| C1 | Cài build mới (B1/B2/B4 + logging-off + upcoming-badge) lên xe owner | ⛔ | | | `~/Desktop/ClusterNav2.0-B-batch-20260819.apk`; theo `docs/diagnostics/oncar-plan-2026-08-19.md`. (B3 đã commit `e914642` + build xanh; rebuild để gồm B3 vô hại — B3 output chưa wire ra cụm nên không đổi hành vi nhìn thấy) |
| C2 | Glyph-test vòng xuyến trên cụm owner — mã CAN nào vẽ directional (15/18/20/24/24+N) trên OEM owner | ⛔ | | | bug owner: vòng xuyến generic = OEM render (app gửi đúng CAN 18); on-car only |
| C3 | HUD provisioning compare — anh em chạy `hud-compare.bat` (USB) → soi cờ | ✅ | 2026-08-19 | 2026-08-19 | **XONG** (compare đã chạy). ⚠ **Đính chính:** HUD anh em hiện nav là **Taobao aftermarket** (render độc lập) → **KHÔNG bác được** `0x38B00030` cho HUD zin; cờ này VẪN là nghi phạm gate (chưa bác). `40d` 138/162 = tình cờ. → `docs/diagnostics/factory-hud-nav-RE-avenues-2026-08-19.md` + `hud-provisioning-compare-2026-08-19.md` (sửa ADR 0002 ×2) |
| C4 | Verify on-car: badge lifecycle over cast; VietMap a11y turn/đường khi dẫn; giá trị VMAlert (upLimit/upDist) | ⛔ | | | sau khi cài C1 |
| C5 | Merge feat → main | ⛔ | | | CHỈ sau khi PASS exact-build on-car + owner duyệt |
| C6 | **Chạy `hud-nav-enable-probe.sh` on-car** (capability + cờ HUD) | ✅ | 2026-08-21 | 2026-08-21 | | C6-LITE CHẠY on-car 08-21 (navopen.jar OPEN nav + full frame, non-hang bg→file): mọi register content rc=0 (0x43E0003A=2·0x4C10E015=3·guide/dist/pathname/eta) NHƯNG **KHÔNG render** ở cụm-centre LẪN HUD (cụm blank 12.8KB, không bị cast chiếm). ⇒ nút thắt = **lớp DISPLAY/render, không phải ghi HAL** → HUD = provisioning-gate (khớp C7/C8); cụm-centre cần app profile-switch(op39/cast)+keep-alive. **Android-lever CẠN — chốt.** getraw/setraw jar (navopen-v4) không có trong repo nên PHASE A/C register-probe không chạy được. **ĐÃ CHẠY on-car 08-21 (navopen-v3 getraw/probe-hud, read-only, no-hang):** SET_HUD_CONFIG 0x38B00015=**1 (xe CÓ HUD W-mode)** NHƯNG HUD_NAV_MAP_CONFIG **0x38B00030=-2147482648 NOT_PROVISIONED** + STATUS 0x30100030 + nav-fusion 0x8e2fcdbf cũng NOT_PROVISIONED; probe-hud chỉ khai feature HUD CONFIG/MODE/SWITCH — **KHÔNG có feature nav**. ⇒ **HUD nav = provisioning-gate MCU, BẤT KHẢ qua Android** (khớp C7/C8). Mở khoá: VDS2100 coding / HUD Taobao (D6). |
| C7 | **Anh em chạy `hud-roadname-test.bat`** trên xe có HUD BYD (đã lên mũi tên+cự ly) — thử **6 encoding tên đường** (`setbytes 0x43FA1008`) + đọc `0x420A1010` check-state | ✅ | 2026-08-19 | 2026-08-20 | **ĐÃ CHẠY** (log `hud-roadname-test_1.txt`): 6/6 **không hiện**; `0x420A1010`=**0** (chưa tới validation, không phải 1/2); **control CJK `五一大道南` cũng không hiện → LOẠI lỗi charset/font**. Tên đường không vào pipeline MCU bằng setbytes đơn lẻ (khớp app). → **follow-up = MA TRẬN probe** (`hud-roadname-matrix.bat`, gói `~/Desktop/HUD-RoadName-Matrix.zip`): BASELINE + 7 lever (DEST_STATUS `43E00038` / DYNAMIC_NAVI `38B0002A` / MAP_TRANSFER `40500025` / ROAD_AHEAD `43F01030` / ADVANCED_ACTION `43F08030` / HUD_MAP_SET `32B1102E` / PASSPOINT `43FFF030`) + COMBO, **log cụm-status từng case** (420A1010 + 38B0002E + 30100030 + dest), reset lever→0 cách ly. Tìm lever nào đổi check-state 0→1/2 / hiện tên đường (= đột phá + fix app). **KẾT QUẢ 08-20 (anh em chạy): NEGATIVE** — BASELINE+L1–L7+COMBO đều `0x420A1010`=**0** (mỗi case CÓ bơm guidance+pathname); `0x38B0002E`/`0x30100030`=**not-provisioned** → **Android-lever CẠN, tên đường bị PROVISIONING-GATE ở MCU** → nhánh VDS2100/D6 (hoặc chấp nhận arrow+dist-only). Chi tiết: `factory-hud-nav-RE-avenues` §08-20(3) |
| C8 | **v3 probe tên đường HUD** — anh em chạy `hud-roadname-v3.bat` | ✅ | 2026-08-20 | 2026-08-21 | Dao sau matrix negative: matrix ghi SET `0x32B1102E` **chưa hề** ghi CONFIG gate `0x38B00030`. v3 = READ-MAP full họ provisioning + **RE-FIRE L1-L7 (7 lever matrix) LOG rc** (giải toả matrix: lever ĐÃ ghi rc=0 vô hiệu hay bị TỪ CHỐI) + **N1 ghi `0x38B00030=1`** + **N2 `0x38B00042` display-content** + N3 map-tips + N4 oversea + N6 strict-sequence, **LOG rc MỌI write** (matrix che `>nul`). Chốt dứt điểm "Android bật provisioning được không". → `factory-hud-nav-RE-avenues §08-20(4)` | **KẾT QUẢ v3 (log `~/byd/hud/hud-roadname-v3.txt`, xem 08-21): ÂM TÍNH DỨT KHOÁT.** N1 ghi `0x38B00030=1` → **rc=-2147482648 (HAL TỪ CHỐI)**, 420A1010=0, tên đường không hiện. N2 `0x38B00042`, N3 `0x40C0B026`, N4 oversea `0x1F7A1008`, L2 `0x38b0002a`, MAP_TRANSFER `0x40500025` — TẤT CẢ rc=-2147482648. Content `0x43E/0x43F` rc=0 nhưng check-state 0x420A1010 **=0 suốt**. ⇒ **Android KHÔNG provision được** (ghi cờ gate bị CHẶN, không chỉ đọc). Chốt: HUD nav = MCU provisioning-gate, cần VDS2100/HUD Taobao (D6). Đóng cả nhánh HUD-via-Android (C6+C7+C8+A11 đồng thuận). |

---

## D. BACKLOG (ưu tiên thấp)

| ID | Việc | Trạng thái | Bắt đầu | Kết thúc | Ghi chú |
|----|------|-----------|---------|----------|---------|
| D1 | Làm mượt nội suy lúc transition/reroute | 📋 | | | hiện đã 95% khớp <1m; ưu tiên thấp |
| D2 | `.bat` auto-reconnect khi WiFi drop | 📋 | | | tiện cho anh em |
| D3 | Viết `docs/diagnostics/hud-provisioning-compare-*.md` sau khi có readback xe anh em | ✅ | 2026-08-19 | 2026-08-19 | `hud-provisioning-compare-2026-08-19.md`. ⚠ kết luận "38B00030 sai" trong doc này **đã bị đính chính** (anh em = HUD Taobao, không bác được cờ zin) → xem `factory-hud-nav-RE-avenues-2026-08-19.md` + ADR 0002 §Sửa lần 2 |
| D6 | **HUD kính ZIN nav — unlock** (nghi phạm gate `0x38B00030`, CHƯA bác). Tổng hợp + xếp hạng đường: `docs/diagnostics/factory-hud-nav-RE-avenues-2026-08-19.md`. **Top đường:** (0) HUD **Taobao** aftermarket = đã chạy với app; (1) sweep 2 fusion switch `0x8e2fcdbf`/`0xd61b6746` [on-car rẻ]; (2) đọc capability `ICarHudService.getHudConfig` HUD zin owner [on-car đọc]; (3) probe `0x32B1102E` [on-car rẻ]; (4) provision `0x38B00030=1` qua UDS coding [tool ngoài]; (5) variant-coding dump [tool ngoài] | 📋 | | | Off-car RE **XONG** (5 track). Đường 1-3 **on-car non-root** làm được; đường 4-5 **NGOÀI scope app** (owner + tool BYD). HUD Taobao ≠ HUD zin. **Chiều-2:** cơ chế = **coding equipment/vehicleCode CỤM** (self-study; `40d` 138=0x8A/162=0xA2 → `38B00030`); UDS on-device CÓ nhưng khoá sau `android.uid.system` → app/dadb chịu, cần **tool dealer**. **Recipe + kết luận cuối:** `docs/diagnostics/hud-coding-recipe-2026-08-19.md` — transport CAN-inject no-root ĐÃ mở, nhưng **3 bí mật (diag-ID cụm / security-key cụm / coding-DID+value) KHÔNG có trong image** (ở MCU firmware/ODX dealer); mở khoá = sniff phiên coding thật / RE MCU firmware / cộng đồng công bố. Không phải "không thể", là "cần nguồn ngoài". **Tool thực tế = `VDS2100`** (Autel, full-coding, tiệm độc lập mua được — dealer từ chối là chính sách) → thợ ghi equipment HUD-nav cụm; **chạy C6 probe trước** để chắc cụm hỗ trợ nav (nếu không → HUD BYD rời). **08-21 ĐÍNH CHÍNH (OWNER): HUD anh em là ZIN gắn thêm (VN-cut, mua HUD zin lắp), KHÔNG Taobao** → HUD zin SL6 hiện arrow+dist dù 0x38B00030=NOT_PROVISIONED ⇒ 0x38B00030 gate ROAD-NAME (không phải arrow+dist); arrow+dist gate = coding `40d` 138(Seal)/162(SL6). Unlock owner = lắp HUD zin + code 138→162 (arrow+dist, proven concept) → road-name cần provision thêm. Xem factory-hud-nav §0.1. **08-20 showroom compare:** Seal owner showroom **KHÔNG demo nav** (SL6 anh em CÓ) → bằng chứng MẠNH firmware HUD Seal **thiếu lớp nav** → coding **KHÔNG THÊM được** feature không có → nghiêng hẳn về **đổi HUD (loại SL6) / HUD rời**, coding chỉ còn cửa nếu C6 báo nav-mode-có-mà-tắt |
| D4 | **Scrub secret CŨ (đã public)** khỏi HEAD + git HISTORY: mật khẩu factory DiLink (`hud-cluster-injection-findings-2026-08-10.md` ×2) + machine-username path (`handoff-2026-08-17-...md`) | 📋 | | | file CŨ đã push từ trước; scrub content hiện tại + `filter-repo` history + force-push (**cần owner duyệt**) |
| D5 | Roundabout icon coverage từ chuyến có nhiều vòng xuyến (đã có data anh em) | 📋 | | | đã đủ coverage; low priority |

---

## E. HỆ THỐNG DOC / KNOWLEDGE (nguồn-sự-thật)

| ID | Việc | Trạng thái | Bắt đầu | Kết thúc | Ghi chú |
|----|------|-----------|---------|----------|---------|
| E0 | Rule Documentation & Backlog (steering, bắt buộc 100%) | ✅ | 2026-08-19 | 2026-08-19 | `.kiro/steering/documentation-and-backlog.md` |
| E1 | **Refactor docs theo rule**: tạo `docs/README.md` INDEX + header mọi doc current + archive doc cũ + đúng 9-loại taxonomy | ✅ | 2026-08-19 | 2026-08-19 | INDEX + header (12 doc) + 9-loại; archive=0 (bảo thủ, chủ đích); docs off-car, **committed c7409b8** |
| E2 | Tạo `.kiro/steering/project-context.md` (kiến trúc + bản đồ nguồn nav + HAL/CAN facts + map file + trạng thái) | ✅ | 2026-08-19 | 2026-08-19 | 43 dòng (<150); khớp diagnostics+backlog; **committed c7409b8**; nhớ `git add -f` (.kiro gitignored) |
| E3 | Index knowledge base: `docs/` (Best) + `core/`+`app/` source (Fast) | ✅ | 2026-08-19 | 2026-08-19 | 3 KB: byd-clusternav-docs + code-core + code-app. **08-21 DEDUP**: gộp worktree gây trùng 2× mỗi context → xoá 6, add lại đúng 3 từ path `byd-cluster-2` (sạch: 1 ecc + 3 byd) |
| E4 | Thêm §Nhật ký triển khai vào spec template + specs hiện có; tạo `docs/decisions/` (ADR) | ✅ | 2026-08-19 | 2026-08-21 | template `_template.html` (§9) + `decisions/` (README+3 ADR) DONE (c7409b8). **08-21 BACK-FILL XONG**: 39 spec thiếu §Nhật ký được thêm honest stub (spec lịch sử → trỏ git/backlog/diagnostics, không bịa); 0 spec còn thiếu. → **E hoàn tất 100%** |

## Ghi chú nguồn nav (tổng hợp — để tham chiếu khi làm B3)
- **GMaps**: notif (nav đầy đủ, chạy NỀN) + arrow bitmap. ✅ đang dùng.
- **VietMap**: widget (speed/limit/upcoming, chạy NỀN) ✅ + a11y content-desc (turn/đường/ETA, **foreground only**) + camera (chỉ trên map SurfaceView → cần B3 screen-capture).
- **Waze/WazeMod**: KHÔNG có kênh data nền; nav = **screen-capture mũi tên (B3, học OpenBYD)** hoặc WazeMod HLP-logcat (cần WazeMod cấu hình) hoặc ESP32 HUD.
